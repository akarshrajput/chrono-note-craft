version: "3"
services:
  vite-app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
# version: "3.8"

# services:
#   app:
#     build: .
#     ports:
#       - "4173:4173"
#     volumes:
#       - .:/app
#     restart: unless-stopped
